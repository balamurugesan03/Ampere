MONGODB_URI=mongodb://localhost:27017/ampere
JWT_SECRET=change-me
JWT_EXPIRES_IN=30d
PORT=4000

# Used by `npm run seed:prod` — must be strong (>=10 chars, not a known default)
ADMIN_SEED_EMAIL=admin@ampere.com
ADMIN_SEED_PASSWORD=
ROOT_SEED_PASSWORD=
